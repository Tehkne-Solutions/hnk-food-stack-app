/**
 * ⚠️ DEPRECATED - Usar src/context/TenantContext.tsx ao invés disso
 * 
 * Este arquivo foi consolidado em src/context/TenantContext.tsx
 * que contém TenantContextProvider e useTenant() com toda a lógica.
 */

// Exportar do arquivo correto para compatibilidade
export { TenantContextProvider as TenantProvider } from '@/context/TenantContext'
export { useTenant } from '@/context/TenantContext'

